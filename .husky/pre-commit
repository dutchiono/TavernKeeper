#!/bin/sh
# Syntax-check the server before committing
echo "🔨 Running pre-commit syntax check..."

node --check server/index.js 2>&1
EXIT_CODE=$?

if [ $EXIT_CODE -ne 0 ]; then
  echo "❌ server/index.js has syntax errors"
  exit 1
fi

echo "✅ Syntax check passed!"
